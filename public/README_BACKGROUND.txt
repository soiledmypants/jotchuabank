Put your puppy photo here and name it:  background.jpg

That's the full-page hero background. If this file is missing the site
falls back to a warm gradient so it never looks broken.

To use a different filename/path, change SITE.backgroundImage in lib/config.ts
